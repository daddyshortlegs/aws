listen_ip = "127.0.0.1"
proxy_url = "http://127.0.0.1:8080"

[storage]
# Directory where QCOW2 files are stored
qcow2_dir = "/tmp/vm-data"
# Directory where VM metadata JSON files are stored
metadata_dir = "/tmp/vm-data"
# Directory where volume metadata, img files, and mounts are stored
volume_data_dir = "/tmp/volume-data"
